# 归档材料

本目录只保存历史审计和过程证据，不参与当前开发契约。归档材料可能包含已修复缺陷、旧接口、旧测试数量或当时的风险判断；使用时必须回到 `docs/current-status.md`、`docs/product-contract.md` 和代码核对。

> [!WARNING]
> 归档中的 Mermaid、ASCII 架构图和数据流图都是当时快照，可能仍包含 React、旧 Flyway 版本、旧 SOAR runtime 或已被替换的一致性路径。不得复制到当前设计或作为代码生成依据；当前图只维护在 `docs/architecture.md`、`docs/architecture-deep-dive.md` 与 `docs/design/soar-runtime-architecture.md`。

归档材料不应被新功能引用，也不应在这里继续追加当前状态。新的验证结论写入当前文档，旧结论只在必要时保留原文。
