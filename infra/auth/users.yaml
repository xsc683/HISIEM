---
# Local HISIEM auth bootstrap seed. Intentionally EMPTY so a fresh control-plane
# (empty PostgreSQL users store) takes the SIEM_BOOTSTRAP_PASSWORD first-run
# bootstrap branch instead of importing a legacy hard-coded user/hash. Real
# users are created at runtime through the official auth API only.
[]
