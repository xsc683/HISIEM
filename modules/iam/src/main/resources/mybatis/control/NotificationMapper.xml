<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xscsiem.hsiem_platform.control.NotificationMapper">
  <resultMap id="notificationRow" type="com.xscsiem.hsiem_platform.control.ControlPlaneRow$NotificationRow">
    <constructor>
      <arg column="id" javaType="java.lang.String" jdbcType="VARCHAR"/>
      <arg column="type" javaType="java.lang.String" jdbcType="VARCHAR"/>
      <arg column="target" javaType="java.lang.String" jdbcType="VARCHAR"/>
      <arg column="message" javaType="java.lang.String" jdbcType="VARCHAR"/>
      <arg column="is_read" javaType="_boolean" jdbcType="BOOLEAN"/>
      <arg column="created_at" javaType="java.time.Instant" jdbcType="TIMESTAMP"/>
    </constructor>
  </resultMap>
  <sql id="notificationColumns">
    id, type, target, message, is_read, created_at
  </sql>
  <select id="selectAllNotifications" resultMap="notificationRow">
    SELECT <include refid="notificationColumns"/> FROM notifications ORDER BY created_at DESC
  </select>
  <select id="selectUnreadNotifications" resultMap="notificationRow">
    SELECT <include refid="notificationColumns"/> FROM notifications
    WHERE is_read = FALSE ORDER BY created_at DESC
  </select>
  <select id="countNotificationsSince" resultType="int">
    SELECT COUNT(*) FROM notifications
    WHERE type = #{type,jdbcType=VARCHAR} AND target = #{target,jdbcType=VARCHAR}
      AND created_at &gt;= #{since,jdbcType=TIMESTAMP}
  </select>
  <insert id="insertNotification">
    INSERT INTO notifications(id, type, target, message, is_read, created_at)
    VALUES (#{id,jdbcType=VARCHAR}, #{type,jdbcType=VARCHAR}, #{target,jdbcType=VARCHAR}, #{message,jdbcType=VARCHAR}, FALSE, CURRENT_TIMESTAMP)
  </insert>
  <update id="markRead">UPDATE notifications SET is_read = TRUE WHERE id = #{id}</update>
  <update id="markAllRead">UPDATE notifications SET is_read = TRUE WHERE is_read = FALSE</update>
  <delete id="deleteById">DELETE FROM notifications WHERE id = #{id}</delete>
  <delete id="deleteReadBefore">
    DELETE FROM notifications WHERE is_read = TRUE AND created_at &lt; #{before,jdbcType=TIMESTAMP}
  </delete>
</mapper>
